item do
  expected do
    url_xpath
    url 'https://git.fedorahosted.org/cgit/linux-pam.git/refs/tags'
    xpath '//*[@id="cgit"]/div/table/tr[2]/td[1]/a'
    regex /^Linux-PAM-([\d.]+)$/
  end

  actual do
    git_tag
    regex /^(.*)-\d+$/
  end
end
